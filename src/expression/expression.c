#include "stack.h"
#include "expression.h"

while (*p)
{
    printf("entered here");
    char ch = *p;
    pintf
}

bool expression_check(const char *exp)
{
    bool status = false;

    return status;
}
